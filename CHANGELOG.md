# Changelog

All notable changes to Helmhost are documented here.
The live version number is **only** in `VERSION` (use `./scripts/hh-version`).

## [Unreleased]

### Added
- Installable packages: macOS `.pkg`, Linux `.deb`, Windows Inno `-setup.exe` (upgradeable)
- FFI copy into Flutter bundles + artifact smoke assert
- Lighthouse era / Lantern codename map (`codenames.toml`) + Waypoint-style `hh-version show`
- Helmhost brand app icons (macOS AppIcon + Hub empty state)
- `scripts/hh-version` for version sync and annotated tags
- Session paint performance, ZRLE, RemoteResize, trackpad scroll, library UI

## [0.1.0] — TBD

| | |
|--|--|
| **Era** | Lighthouse |
| **Track** | foundation |
| **Codename** | Lantern (`lantern`) |
| **Public** | Helmhost Lighthouse — Lantern (v0.1.0) |
| **Was** | — (first tagged baseline) |

First tagged desktop viewer baseline.
