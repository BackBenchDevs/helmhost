# Update-signing keys (do not commit private keys).
#
# macOS / Sparkle — Ed25519 (EdDSA)
#   Public:  apps/client/macos/Runner/Info.plist → SUPublicEDKey
#            packaging/sparkle/SUPublicEDKey.txt
#   Secret:  GitHub Actions SPARKLE_ED_PRIVATE_KEY
#            (contents of packaging/sparkle/ed25519_priv.key — gitignored)
#
# Windows / WinSparkle 0.8 — DSA-4096 (library only supports DSA+SHA1)
#   Public:  apps/client/windows/dsa_pub.pem (+ Runner.rc DSAPub)
#   Secret:  GitHub Actions WINSPARKLE_DSA_PRIVATE_KEY
#            (contents of packaging/winsparkle/dsa_priv.pem — gitignored)
#
# Feed URLs (stable CDN — published by Release → Pages, not by merging XML into master):
#   stable → https://docs.helmhost.bbdevs.com/appcast.xml
#            (also https://backbenchdevs.github.io/helmhost/appcast.xml)
#   rcs    → https://docs.helmhost.bbdevs.com/appcast-rcs.xml
#
# Immutable installers live on GitHub Releases. Mutable feeds live on Pages only.
# docs/appcast/*.xml in git are stubs — see docs/appcast/README.md.
