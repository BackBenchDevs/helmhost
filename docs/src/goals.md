# Goals

- Hub + N session windows
- Pure Rust RFB MVP (ZRLE, DesktopSize / RemoteResize)
- Input grab / release; credentials not logged
- Single version SSOT (`VERSION` + `codenames.toml` + `hh-version` CLI)
